- defaults:
    link:
      relink: true
      create: true

- clean: [ '~', '~/.config' ]

- link:
    ~/.gitignore_global:
      path: git/gitignore_global
    ~/.gitconfig:
      path: git/gitconfig

    ~/.vimrc:
      path: vimrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
