- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.spacemacs: 
    ~/.bash_profile: 
    ~/.gitignore_global:


- shell:
  - [git submodule update --init --recursive, Installing submodules]
